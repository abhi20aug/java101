package com.java101.sec25.collection.part2.list;

import java.util.*;

public class Example241_List {

    //    List list = new List()

    //  add(int index, E e);
    //  insert k at 4th position
    //  add(4,k)

    //  add more than 1 value starting at some value
    //  addAll(4, list2)

    //remove(4,k) //index,object
    //get(int index)
    //set(int index, E e) //E is a Type which may be a class or primitive type also.

    //sublist(int idxFrom, int idxTo) //get part of list
    //indexOf(Object obj) //left2right search
    //lastIndexOf(Object obj)
    //indentfy duplicate by using above.

    //listIterator() access Object in any direction
    //listIterate(int index)

    //interface Set extends Collection


    //Queue
        //interface Queue extends Collection
        //add(E e) always added at end.
        //poll()    //remove first element, if empty, returns null
        //remove()  //remove first element, if empty throws exception.
                    //NoSuchElementException
        //peek()    //object present at first position, if empty returns null
        //element() //object present at first position, if empty NoSuchElementException

}
